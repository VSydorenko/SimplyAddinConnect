# Компонента AddinUAPKIConnect (ЕЦП/крипто) — інструкція для 1С-розробника

Компонента для **електронного підпису й шифрування** українським стеком **UAPKI**
(КЕП/ЕЦП ДСТУ 4145, ДСТУ 7624/7564, формати CAdES, контейнери PKCS#12). Клас компоненти в
1С — **`AddinUAPKIConnect`**.

На відміну від драйверів обладнання ([ECRPrivatJSON](ecr-privatjson.md),
[LabelPrinter](label_printer.md)), це **не** БПО-драйвер. Компонента — тонкий фасад над
бібліотекою UAPKI з **єдиною точкою входу** `ВызватьUAPKI(Метод, JSONПараметри)`, яка повертає
**JSON-рядок** відповіді бібліотеки. Уся семантика — це JSON-протокол UAPKI (методи, параметри,
коди помилок); повний опис — авторитетна настанова бібліотеки
[`extern/uapki/doc/UAPKI-PM-2.0.16.md`](../../extern/uapki/doc/UAPKI-PM-2.0.16.md).

Спільні механізми (підключення DLL, кеш компоненти, логування) — у [README.md](README.md);
тут — специфіка саме UAPKI.

> **Провайдер НКІ вбудований у DLL.** Крипто-провайдер `cm-pkcs12` (робота з файловими ключами
> `.p12`/`.pfx`) вкладено в головну DLL ресурсом і компонента розгортає його сама при `INIT` —
> **ставити додаткові файли не треба**. Внутрішня механіка — [docs/architecture/uapki.md](../architecture/uapki.md).

---

## 1. Швидкий старт

```bsl
&НаКлиенте
Перем ОбъектUAPKI;   // одна instance на форму — стан бібліотеки (ініціалізація, відкритий НКІ) живе в C++

// 1) Підключити компоненту (README §2) → маємо СимвольноеИмя, напр. "SimplyAddinConnect"
// 2) Створити об'єкт ОДИН раз:
ОбъектUAPKI = Новый("AddIn." + СимвольноеИмя + ".AddinUAPKIConnect");
ОбъектUAPKI.ИспользоватьЛогирование("Trace", "C:\log\SimplyAddinConnect.log");

// 3) Ініціалізація бібліотеки (провайдер cm-pkcs12 розгортається сам):
Відповідь = РаскодироватьJSON(ОбъектUAPKI.ВызватьUAPKI("INIT", "{""offline"":true}"));
Если Відповідь.errorCode <> 0 Тогда Сообщить("INIT: " + Відповідь.error); Возврат; КонецЕсли;
Если Відповідь.result.countCmProviders = 0 Тогда
    Сообщить("Провайдер НКІ не завантажився — підпис приватним ключем недоступний"); Возврат;
КонецЕсли;

// 4) Далі: OPEN контейнера → SELECT_KEY → SIGN → VERIFY → CLOSE → DEINIT (§4)
```

---

## 2. Єдина точка входу — `ВызватьUAPKI`

| Метод (EN / RU) | Параметри | Повертає |
|---|---|---|
| `CallUapki` / `ВызватьUAPKI` | `Метод:Рядок`, `JSONПараметри:Рядок` | **Рядок** (JSON-відповідь UAPKI) |

- **`Метод`** — назва методу UAPKI (`INIT`, `OPEN`, `SIGN`, `VERIFY`, …; регістр методу
  бібліотека не розрізняє для `INIT`, решту пиши як у протоколі — великими).
- **`JSONПараметри`** — параметри методу. Приймаються у **двох форматах**:
  - **готовий JSON-об'єкт** (рядок починається з `{`) — передається в бібліотеку як поле
    `parameters` без змін (єдиний спосіб задати вкладені структури — `cmProviders`, `signParams`,
    `dataTbs`, `trustedCerts`);
  - **плоский** `ключ=значення,ключ=значення` — зручно для простих методів без вкладень.
- **Повертає JSON-рядок** відповіді бібліотеки (не Булево). Розбирай штатним `ЧтениеJSON`.

> **Порожні параметри дозволені:** `ВызватьUAPKI("DEINIT", "")`, `ВызватьUAPKI("VERSION", "")`.
> Для `INIT` порожній рядок = «параметри за замовчуванням» (компонента однаково сама інжектить
> `cmProviders`, див. §5).

Хелпер розбору відповіді (використовується скрізь нижче):

```bsl
&НаКлиенте
Функция РаскодироватьJSON(ТекстJSON)
    Читання = Новый ЧтениеJSON;
    Читання.УстановитьСтроку(ТекстJSON);
    Возврат ПрочитатьJSON(Читання);
КонецФункции

// Виклик методу + розбір + перевірка успіху в одному місці
&НаКлиенте
Функция UAPKI(Метод, JSONПараметри = "")
    Стр = РаскодироватьJSON(ОбъектUAPKI.ВызватьUAPKI(Метод, JSONПараметри));
    Возврат Стр;   // Стр.errorCode = 0 — успіх; інакше Стр.error — опис
КонецФункции
```

---

## 3. Формат відповіді

Усі методи повертають єдиний конверт (протокол UAPKI):

```json
{ "errorCode": 0, "method": "INIT", "result": { }, "error": "" }
```

| Поле | Тип | Зміст |
|---|---|---|
| `errorCode` | Число | **`0` — успіх**; інше — код помилки (Додаток А протоколу) |
| `method` | Рядок | ехо назви методу |
| `result` | Структура | вихідні дані методу (склад залежить від методу) |
| `error` | Рядок | короткий опис помилки (лише за `errorCode <> 0`) |

**Успіх операції визначай ТІЛЬКИ за `errorCode = 0`** (а не за наявністю `result`).

Зворотне, однак, **не діє**: `errorCode = 0` означає лише, що метод відпрацював без помилки, — воно
**не** гарантує, що в `result` є потрібне тобі поле. Два місця, де це кусається, розписані окремо:
`SELECT_KEY` без `certId` (§4.3) і `INIT` з `countCmProviders = 0` (§5). В обох бібліотека вважає
свою роботу виконаною, а прикладний сценарій — ні.

---

## 4. Життєвий цикл: `INIT → OPEN → SELECT_KEY → SIGN → VERIFY → CLOSE → DEINIT`

> **Три правила бібліотеки (важливо):**
> - **`INIT` — один раз на процес.** Повторний `INIT` без `DEINIT` дасть помилку
>   `RET_UAPKI_ALREADY_INITIALIZED`. Не ініціалізуй на кожну операцію.
> - **Один відкритий НКІ й один вибраний ключ водночас** — стан глобальний для всіх потоків.
>   Щоб узяти інший ключ — спершу `SELECT_KEY` на нього.
> - **Тримай об'єкт живим** між викликами (стан ініціалізації/відкритого НКІ — у C++).

### 4.1. `INIT` — ініціалізація

Провайдери НКІ компонента інжектить сама (§5) — зазвичай досить передати лише режим:

```bsl
Р = UAPKI("INIT", "{""offline"":true}");
Если Р.errorCode <> 0 Тогда Сообщить("INIT: " + Р.error); Возврат; КонецЕсли;
Сообщить("Завантажено провайдерів НКІ: " + Р.result.countCmProviders);   // має бути >= 1
```

Поля `result` `INIT`: `countCmProviders` (скільки провайдерів завантажено — **звіряй з 0**,
див. §5), `offline`, `certCache`, `crlCache` тощо.

### 4.2. `OPEN` — відкрити контейнер ключа (`.p12`/`.pfx`)

```bsl
ПараметриOpen = "{""provider"":""PKCS12"","
    + """storage"":""C:\\keys\\key.p12"","
    + """password"":""pin-код"","
    + """mode"":""RO""}";
Р = UAPKI("OPEN", ПараметриOpen);
Если Р.errorCode <> 0 Тогда Сообщить("OPEN: " + Р.error); Возврат; КонецЕсли;
```

- `provider` — `"PKCS12"` для файлових контейнерів.
- `storage` — шлях до файлу (у JSON зворотні слеші подвій: `C:\\keys\\key.p12`).
- `password` — пароль/PIN контейнера.
- `mode` — `"RO"` (лише читання/підпис), `"RW"`, `"CREATE"` (новий контейнер).
- Ключі з пам'яті: `storage:"file://memory"` + `openParams.bytes` (Base64 вмісту `.p12`).

### 4.3. `KEYS` → `SELECT_KEY` — вибрати активний ключ

```bsl
Ключі = UAPKI("KEYS", "");            // Ключі.result.keys — масив {id, keyId2, label, signAlgo, ...}
Ключ  = Ключі.result.keys[0];         // або обери потрібний за label

// ДСТУ-ключ має ДВА ідентифікатори (врізка нижче) — купинний пробуємо першим
Кандидати = Новый Массив;
Если Ключ.Свойство("keyId2") Тогда Кандидати.Добавить(Ключ.keyId2); КонецЕсли;
Кандидати.Добавить(Ключ.id);

СертифікатЗнайдено = Ложь;
Для Каждого Ид Из Кандидати Цикл
    Р = UAPKI("SELECT_KEY", "{""id"":""" + Ид + """}");
    // errorCode = 0 тут НЕ означає, що сертифікат знайдено: ключ без сертифіката легальний
    Если Р.errorCode = 0 И Р.result.Свойство("certId") Тогда
        СертифікатЗнайдено = Истина;
        Прервать;
    КонецЕсли;
КонецЦикла;

Если НЕ СертифікатЗнайдено Тогда
    Сообщить("Ключ вибрано, але сертифіката до нього немає — SIGN з includeCert:true дасть 4161");
КонецЕсли;
```

Після `SELECT_KEY` у `result` повертається сертифікат ключа (`certId` + `certificate`, Base64) —
якщо він є в контейнері/кеші **й знайшовся за переданим ідентифікатором** (див. врізку).

> **Пастка двох ідентифікаторів (тільки ДСТУ) — читай, навіть якщо зараз усе працює.**
> ДСТУ-ключ має **два** ідентифікатори: `id` (геш ГОСТ 34311 відкритого ключа) і `keyId2`
> (Купина-256 того самого ключа). Сертифікати **нового зразка** несуть `SubjectKeyIdentifier`
> **на Купині**, а бібліотека шукає сертифікат за тим самим ідентифікатором, яким ти покликав
> `SELECT_KEY`. Тому виклик зі звичним `id` на новому ключі:
> - **ключ вибере** — сховище приймає обидва ідентифікатори;
> - **сертифікат не знайде** і поверне **`errorCode: 0` без поля `certId`**. Це легальна поведінка
>   бібліотеки (ключ без сертифіката дозволений), а не помилка, тому в лог нічого не потрапить;
> - а впаде вже наступний `SIGN` з `includeCert: true` — непрозорим **`4161 CERT_NOT_FOUND`**,
>   при тому що сертифікат Є і в контейнері, і в кеші.
>
> З **01.09.2026** усі КНЕДП зобов'язані видавати ключі нового зразка, тож на першому ж перевипуску
> ключа звичний шлях `keys[0].id → SELECT_KEY → SIGN` перестане працювати — і виглядатиме це як
> дефект компоненти, що з'явився нізвідки. **Два правила:** бери `keys[].keyId2`, коли він є, і
> перевіряй **наявність `certId`** у відповіді `SELECT_KEY`, а не `errorCode = 0`. Механізм —
> [architecture/uapki.md](../architecture/uapki.md) §9.1.

### 4.4. `SIGN` — підписати дані

Дані передаються масивом `dataTbs`, кожен елемент — Base64 (`bytes`). Формат підпису й опції —
у `signParams`/`options`:

```bsl
// Дані для підпису → Base64
ДвоичныеДанные = ПолучитьДвоичныеДанныеИзСтроки("Текст для підпису");
ДаніBase64 = Base64Строка(ДвоичныеДанные);

ПараметриSign = "{""signParams"":{"
    + """signatureFormat"":""CAdES-BES"","        // BES (офлайн) | T (з позначкою часу, потребує TSP/онлайн)
    + """signAlgo"":""1.2.804.2.1.1.1.1.3.1.1"","  // ДСТУ 4145
    + """detachedData"":false,"                    // false = дані всередині підпису (enveloping)
    + """includeCert"":true,"                      // вкласти сертифікат підписувача
    + """includeTime"":true},"                     // signingTime від локального годинника (§8-біс)
    + """dataTbs"":[{""id"":""doc-0"",""bytes"":""" + ДаніBase64 + """}],"
    + """options"":{""ignoreCertStatus"":true}}";  // не звіряти статус сертифіката (тест/офлайн)

Р = UAPKI("SIGN", ПараметриSign);
Если Р.errorCode <> 0 Тогда Сообщить("SIGN: " + Р.error); Возврат; КонецЕсли;
ПідписBase64 = Р.result.signatures[0].bytes;      // готовий CMS-підпис (Base64)
```

### 4.5. `VERIFY` — перевірити підпис

**Холістичний вердикт бери з `signatureInfos[0].status`, а НЕ з `statusSignature`** — останній
лишається `"VALID"` навіть при пошкодженому detached-контенті.

> **Але `TOTAL-VALID` у режимі `STRUCT` ≠ «сертифікат чинний».** `STRUCT` — офлайнова **структурна**
> перевірка: вона підтверджує, що підпис і геш вмісту цілі, і **не перевіряє ані ланцюг довіри, ані
> строк дії сертифіката** (ланцюг у цьому режимі взагалі не будується, тож і прострочення нема де
> помітити). Виміряно на еталонах ДПС: `запит_стану_РРО.json.signed` наш двигун приймає як
> `TOTAL-VALID`, а незалежний двигун ІІТ відхиляє з кодом `52` — «Сертифікат не чинний за строком
> дії». Тобто **ми приймаємо те, що незалежний двигун відхиляє**.
>
> Практичне правило: `TOTAL-VALID` у `STRUCT` означає «**структурно** валідний», а не «юридично
> чинний». Хто прочитає рекомендацію вище буквально — прийме документ, підписаний простроченим
> сертифікатом. Якщо за вердиктом стоїть юридичне рішення, `STRUCT` недостатньо: потрібен
> `"validationType": "CHAIN"` (ланцюг + строк дії) або `"FULL"` (додатково OCSP/CRL), а отже —
> наповнені `certCache`/`trustedCerts`, а для `FULL` ще й мережа. Для ПРРО ми цього свідомо не
> робимо — рішення й умови його перегляду в [architecture/uapki.md](../architecture/uapki.md) §8.3,
> механізм `STRUCT` — там само §9.2.

```bsl
ПараметриVerify = "{""signature"":{""bytes"":""" + ПідписBase64 + """},""options"":{""validationType"":""STRUCT""}}";   // enveloping + структурна (офлайн) перевірка
Р = UAPKI("VERIFY", ПараметриVerify);
Если Р.errorCode = 0 И Р.result.signatureInfos[0].status = "TOTAL-VALID" Тогда
    Сообщить("Підпис валідний");
Иначе
    Сообщить("Підпис НЕ валідний: " + ?(Р.errorCode = 0, Р.result.signatureInfos[0].status, Р.error));
КонецЕсли;
```

### 4.6. `CLOSE` / `DEINIT`

```bsl
UAPKI("CLOSE", "");    // закрити НКІ (за потреби перед відкриттям іншого)
UAPKI("DEINIT", "");   // деініціалізація бібліотеки (в кінці роботи)
```

---

## 5. Провайдери НКІ — самодоставка (нічого налаштовувати не треба)

Робота з приватними ключами (`OPEN`/`SIGN`) в UAPKI виконується **окремим провайдером НКІ**
(`cm-pkcs12`). Компонента робить це **прозоро**:

- при `INIT` вона сама додає в параметри `cmProviders` (каталог + `cm-pkcs12` з арх-суфіксом),
  тож **власноруч задавати `cmProviders` не потрібно**;
- сам файл провайдера **вбудований у DLL** і розгортається компонентою при першому `INIT`
  (у `%LOCALAPPDATA%\SimplyAddinConnect\providers\<версія>\`) — жодних додаткових DLL коло
  бази 1С класти не треба.

**Єдина перевірка з боку 1С:** після `INIT` подивись `result.countCmProviders`. Якщо `0` — провайдер
не завантажився (антивірус заблокував запис/завантаження, немає прав на `%LOCALAPPDATA%` тощо), і
`OPEN`/`SIGN` не працюватимуть. Сама бібліотека при цьому все одно віддає `errorCode:0` — тому
`countCmProviders` і є справжнім індикатором готовності.

Якщо треба вказати власний каталог провайдерів (напр. апаратний токен `cm-pkcs11`) — передай
`cmProviders.dir` явно в параметрах `INIT`: непорожній `dir` компонента поважає як є (розгортання
вбудованого ресурсу не виконується).

---

## 6. Офлайн-режими (без доступу до мережі)

Тестові/локальні сценарії часто без доступу до OCSP/CRL/TSP:

- `"offline": true` у **`INIT`** — вимикає онлайн-перевірки статусу сертифікатів.
- `"options": {"ignoreCertStatus": true}` у **`SIGN`** — не звіряти статус сертифіката (потрібно
  для тестового/простроченого ланцюга).
- **`VERIFY` у режимі STRUCT** (значення за замовчуванням, коли `validationType` не задано) — чисто
  структурна офлайн-перевірка підпису: ані ланцюга, ані строку дії сертифіката. Що з цього випливає
  для трактування вердикту — врізка в §4.5.
- Онлайн-формат в офлайні (напр. **CAdES-T**, якому потрібен TSP) → помилка
  `RET_UAPKI_OFFLINE_MODE` (код `4120`) — **очікувана поведінка**, не збій.

---

## 7. Помилки

- **Помилки бібліотеки:** `errorCode <> 0` + `error` (короткий опис). Повний перелік кодів —
  **Додаток А** настанови [`extern/uapki/doc/UAPKI-PM-2.0.16.md`](../../extern/uapki/doc/UAPKI-PM-2.0.16.md).
  Часті: `4120` (`RET_UAPKI_OFFLINE_MODE` — онлайн-операція в офлайні), `RET_UAPKI_ALREADY_INITIALIZED`
  (повторний `INIT`), помилки пароля/формату контейнера в `OPEN`.
- **Помилки рівня компоненти** (виняток C++, некоректний виклик) повертаються у тому ж форматі з
  `errorCode: 500` — поля `errorCode` і `error` (порядок серіалізації не є частиною контракту,
  читати за іменем поля, а не за позицією), напр. `{"error":"<текст>","errorCode":500}`.

```bsl
Р = UAPKI("OPEN", ПараметриOpen);
Если Р.errorCode <> 0 Тогда
    Сообщить("UAPKI помилка [" + Строка(Р.errorCode) + "]: " + Р.error);
КонецЕсли;
```

---

## 8. Підпис документів ПРРО (фіскальні чеки/Z-звіти)

Для ЄВПЕЗ (фіскальний сервер ДПС) підпис має бути **enveloping** (дані всередині), із **вкладеним
сертифікатом підписувача**, але **без** CRL/OCSP/ланцюга видавця й **без** content-timestamp.

**Щодо позначки часу норматив і бойова практика розходяться — і це свідомий відступ, а не недогляд.**
Норматив ДПС вимагає `CAdES-E-T` із `signature-time-stamp`. Бойова система працює на `CAdES-BES` +
`includeTime` (локальний час), і ДПС такі підписи **приймає**, видаючи фіскальні номери — це виміряно,
а не припущено. Саме рішення, його підстава й **умова перегляду** зафіксовані в
[architecture/uapki.md](../architecture/uapki.md) §8.2; природа часу в підписі — §8-біс нижче.

Робочий профіль (те, що працює сьогодні):

```jsonc
"signParams": {
  "signatureFormat": "CAdES-BES",  // без TSP: працює офлайн, ДПС приймає
  "detachedData": false,           // enveloping — дані всередині
  "includeCert": true,             // сертифікат підписувача — так
  "includeContentTS": false,       // content-timestamp — ні
  "includeTime": true              // signingTime від локального годинника (НЕ довірений час, §8-біс)
}
```

Нормативний профіль виходить заміною одного поля — `"signatureFormat": "CAdES-T"`, — але він
потребує **онлайн-доступу до TSP на кожен підпис** (в офлайні дасть `4120`, §6). Два застереження:
TSP-адресу бібліотека бере **з сертифіката підписувача**, тобто йде в TSP того КНЕДП, що видав ключ,
а не ДПС; і цей шлях **не покритий жодним тестом** — [architecture/uapki.md](../architecture/uapki.md) §8.1.

Точні вимоги до підпису, еталони й крос-валідацію див. у скілі
[`.claude/skills/prro-fiscal`](../../.claude/skills/prro-fiscal/SKILL.md); формати підпису — Додаток Б
настанови UAPKI.

---

## 8-біс. Час підпису: три режими, і лише один із них довірений

| Режим | Джерело часу | Довіра | Мережа на кожен підпис | Як задати |
|---|---|---|---|---|
| локальний | годинник машини | ні | немає | `signParams.includeTime: true` |
| заданий ззовні | те, що передала 1С | ні | немає | `signedAttributes` з OID `1.2.840.113549.1.9.5` + `includeTime: false` |
| довірена позначка | TSP-сервер (RFC 3161) | **так** | так | `signatureFormat: "CAdES-T"` |

**Головне, щоб не помилитись:** «час із сервера» **не робить позначку довіреною**. `signingTime`
— це підписаний атрибут, тобто **твердження підписувача**; звідки взято значення, на його
юридичний статус не впливає. Довіру дає лише TSP-токен, бо там час засвідчує третя сторона.

**Що відтворює поточну бойову поведінку:** перший рядок. Виміряно на боці 1С: підпис іде з
`signingTime` від локального годинника, а `signature-time-stamp` відсутній повністю.

Компонента для всіх трьох режимів **правок не потребує** — вона passthrough, і `signParams`
повністю під контролем 1С.

**Діагностика зсуву годинника** належить туди, де вже відбувається HTTP-обмін — на бік 1С,
через заголовок `Date` у відповіді фіскального сервера. Це саме той годинник, за яким нас
судять, і він приходить безкоштовно з відповіддю, яку й так отримуємо.

> **Куди цей час дівається при перевірці.** У режимі `VERIFY` `CHAIN`/`FULL` строк дії сертифіката
> звіряється не з поточним часом, а з «найкращим часом підпису»: за наявності TSP-токена — з часом
> токена, інакше — саме з цим `signingTime`, тобто з твердженням підписувача. Ще одна причина, чому
> локальний час перевіркою не є. Механізм — [architecture/uapki.md](../architecture/uapki.md) §9.2.

---

## 9. Тестування без 1С і з 1С (тест-контур)

Крипто-стек має **автоматичний тест-контур** (збирається лише разом із `-WithUAPKI`), яким можна
відлагодити ланцюг ЕЦП без 1С:

- **`uapki_selftest.exe`** (L1) — крипто-ядро через `process()` за JSON-сценаріями
  [`tests/scenarios/*.json`](../../tests/scenarios) (`INIT`/`OPEN`/`SIGN`/`VERIFY`/… на тестовому
  контейнері `test-diia.p12`, пароль `testpassword`).
- **`native_host.exe`** (L2/L3) — компонента `AddinUAPKIConnect` **через головну DLL** (як 1С):
  розгортання провайдера ресурсом, повний ланцюг підпис→перевірка, крос-валідація еталонів ПРРО.

Запуск гейта: `build_project.ps1 -WithUAPKI -WithTests`, потім `run_tests.ps1 [x64|x86]`. Точний
склад — [AGENTS.md](../../AGENTS.md) «Тести»; ноу-хау тестування ЕЦП без 1С — скіл
[`.claude/skills/ecp-testing-without-1c`](../../.claude/skills/ecp-testing-without-1c/SKILL.md).

> Тестовий ланцюг сертифікатів **прострочений** (тому в сценаріях скрізь `offline:true` +
> `ignoreCertStatus:true`, а `VERIFY` — у режимі STRUCT). Для бойового підпису потрібні чинні
> ключ і сертифікат.

### 9.1. `uapki_fiscal_emulator` — HTTP-оракул ЕЦП для тесту з РЕАЛЬНОЇ 1С

> **Статус: перевірено наскрізь із реальної 1С (2026-07-31, збірка 3.0.2.119).** Пройшли обидва
> напрямки: підпис компонентою → `POST /doc` → оракул `ACCEPTED status=TOTAL-VALID` → підписана
> квитанція назад → 1С розгорнула `<TICKET>`; і `GET /reference` → еталон від оракула → `VERIFY`
> компонентою `TOTAL-VALID`.
>
> **Три умови, без яких воно не запрацює** (усе вже налаштовано в тестовій обробці):
> 1. **`INIT` мусить отримати `certCache`** — сертифікат підписувача тест-ключа лежить у
>    `tests/data/certs/`, а НЕ в `.p12`; без нього `SIGN` з `includeCert:true` дає
>    `4161 CERT_NOT_FOUND`. У обробці за це відповідає реквізит «Каталог даних UAPKI».
> 2. **`SELECT_KEY` обов'язковий** — `KEYS` лише перелічує ключі; без вибору `SIGN` дає
>    `4108 NO_KEY`.
> 3. **Порт консолі має збігатися** з тим, що надрукував оракул: дефолт `8099`, але при зайнятому
>    порту він бере наступний вільний і повідомляє про це.

UAPKI — не мережевий пристрій, а in-process крипто-бібліотека: підпис/перевірка відбуваються
всередині процесу 1С, і «дроту» назовні, який можна було б підмінити емулятором, немає. Тому
`tests/uapki_fiscal_emulator.exe` (ручна ціль, збирається лише з `-WithUAPKI -WithTests`, у гейт
`run_tests.ps1` не входить) грає не пристрій, а **приймаючу сторону** — сервер ДПС/ЄВПЕЗ:
1С підписує документ компонентою й шле CMS по HTTP, консоль **незалежним шляхом інтеграції**
(`process()`/`json_free()` статично злінкованого ядра — той самий шлях, що й `uapki_selftest`)
перевіряє підпис і повертає підписану квитанцію, яку 1С розгортає назад компонентою. Це
**cross-path-валідація** шарів маршалінгу/серіалізації (компонента ↔ консоль), а не незалежна
крипто-реалізація — крипто-рушій той самий.

**CLI:**

```
uapki_fiscal_emulator [port] [--key <p12>] [--pass <pwd>] [--providers <dir>]
                      [--data <dir>] [--samples <dir>] [--canned] [--self-test]
```

| Параметр | Зміст | Дефолт |
|---|---|---|
| `port` | порт HTTP-оракула | `8099`; **якщо зайнятий і порт не задано явно — автоматично береться наступний вільний** (до `+10`), про що консоль голосно повідомляє: саме цей номер треба вписати в поле «Порт консолі» обробки. Заданий явно порт НЕ підмінюється |
| `--key` | PKCS#12-контейнер тест-ключа | `<data>\test-diia.p12` |
| `--pass` | пароль контейнера | `testpassword` |
| `--providers` | каталог із `cm-pkcs12_*.dll` | каталог самого exe (`bin/Release`) |
| `--data` | каталог тест-даних `certs/` + `crls/` | compile-time `tests/data` |
| `--samples` | каталог зразків ПРРО (`.signed`) для `--canned` | `%PRRO_DOCS_DIR%` або `R:\github\prro_docs\...\Приклади з КЕП` |
| `--canned` | `/reference` віддає канонічний `.signed`-зразок замість свіжо-підписаного | вимкнено |
| `--self-test` | прогнати вбудовані перевірки (сервер НЕ піднімається) і вийти | вимкнено |

Коди повернення: `0` — штатно; `1` — не піднявся порт / провал `--self-test`; `2` — помилка CLI/
bootstrap (провайдер/ключ не відкрились).

> **Запуск подвійним кліком із Провідника.** Якщо консоль запущено власним вікном, при виході з
> помилкою вона чекає `Enter` — інакше вікно зникало б раніше, ніж можна прочитати причину.
> Із cmd/PowerShell/CI паузи немає.

**Ендпоінти:**

| Метод/шлях | Вхід | Дія | Вихід |
|---|---|---|---|
| `GET /ping` | — | перевірка живості | `200`, текст |
| `POST /doc` | тіло = DER CMS | VERIFY (критерій нижче) → підписує квитанцію (`ticket01`-подібний XML) | `200` (прийнято) / `422` (відхилено) + тіло = DER CMS квитанції |
| `POST /verify` | тіло = DER CMS | VERIFY без квитанції | `200`/`422` + JSON `{accepted, status, statusSignature, statusMessageDigest, validSignatures, validDigests, certEmbedded, signerCertId, contentBase64, errorCode, errorText}` |
| `GET /reference?type=check\|zrep\|ticket` | — | віддати еталонний підписаний документ | `200` + тіло = DER CMS |

Додаткові коди: `400` — порожнє/некоректне тіло; `404` — невідомий шлях або невідомий `type`
(і `type=ticket` при `--canned` — канонічного зразка квитанції немає); `413` — тіло понад ліміт
1 МіБ (норматив ЄВПЕЗ обмежує пакет 200 КБ, ліміт консолі — із запасом); `500` — внутрішня помилка
UAPKI (не вдалось підписати квитанцію/еталон).

> **Два режими `/reference` — і що вони насправді віддають.** Обидва повертають `200` + DER CMS;
> різниця — у ТОМУ, ЩО підписано, і чи пройде воно `VERIFY` на боці 1С.
> - **Без `--canned`** (дефолт) — консоль підписує **синтетичний каркас** (`<CHECK>reference</CHECK>`,
>   `<ZREP>reference</ZREP>`, `ticket01`-XML) власним тест-ключем `test-diia.p12` у CAdES-BES.
>   Це не реальний документ ПРРО, а саме каркас — але **лише цей режим** дає позитивний
>   round-trip: 1С успішно перевіряє (`TOTAL-VALID`) і показує вміст.
> - **З `--canned`** — консоль віддає **справжні файли ДПС** із «Прикладів з КЕП» як є, без
>   перепідпису (`type=ticket` у цьому режимі — `404`: канонічного зразка квитанції не існує).
>   Ці еталони підписано з **позначкою часу (CAdES-T)**, а UAPKI бракує сертифіката
>   **TSP-сервера АЦСК** — у детальній відповіді видно
>   `"expectedCerts":[{"entity":"TSP","keyId":"66ECEB8E…"}]`.
>   Тому `VERIFY` (кнопка в 1С або `POST /verify`) повертає
>   `errorCode = 4161` (`CERT_NOT_FOUND`), і вміст не покажеться — **це очікувано**.
>   Сертифікат ПІДПИСУВАЧА там при цьому Є (`certIds` непорожній, `signerCertId` знайдено,
>   `statusSignature:"VALID"`, `validDigests:true`), але `status:"INDETERMINATE"` і
>   `validSignatures:false` → холістичний критерій (нижче) не виконується.
>
> Це властивість вхідних файлів, а не дефект консолі чи компоненти: ті самі файли дають той самий
> `errorCode=4161` і в `native_host` (кейс 5) — тобто через головну DLL, з `INIT` взагалі без
> тестового `certCache`; натомість третій зразок `запит_стану_РРО.json.signed` (він без позначки
> часу) проходить STRUCT-перевірку як `TOTAL-VALID`. Саме цей зразок і показує ціну такого вердикту:
> незалежний двигун ІІТ відхиляє його як підписаний **простроченим** сертифікатом (код `52`) —
> див. врізку в §4.5. Режим `--canned` корисний як негативний кейс і як джерело реальних
> DER-структур ПРРО; для позитивного round-trip запускай консоль **без** `--canned`.

**Критерій прийняття VERIFY** (той самий, що й у §4.5, лише розписаний холістично): документ
приймається `/doc`/`/verify`, тільки якщо **одночасно** `errorCode==0`, `status=="TOTAL-VALID"`,
`validSignatures==true` **і** `validDigests==true` — самого `statusSignature=="VALID"`
**недостатньо**, бо він лишається `"VALID"` навіть при пошкодженому вкладеному вмісті (перевірку
цього ловить саме `status`/`validDigests`). У відповіді `/verify` додатково повертається
діагностичний `certEmbedded` (сертифікат підписувача реально вкладено в CMS, а не лише знайдений
у кеші) — сам факт наявності `signerCertId` цього не доводить.

`errorCode`/`errorText` — сира причина відмови від крипто-ядра (напр. `4161 CERT_NOT_FOUND`).
**Якщо `errorCode != 0`, решта полів не заповнюється**: `status`, `statusSignature`,
`statusMessageDigest`, `signerCertId`, `contentBase64` — порожні, а `validSignatures`,
`validDigests`, `certEmbedded` — `false` (консоль виходить одразу після розбору конверта
відповіді). Тож при відмові на рівні розбору/валідації CMS саме ця пара — єдине джерело
діагностики; вона ж друкується в трейс консолі рядком `причина: VERIFY errorCode=…` — і для
`/verify`, і для `/doc` (у самому трейсі `status` тоді показується як `(немає)`).

`--self-test` прогонить це без 1С: валідний свіжо-підписаний CMS приймається; CMS зі зіпсованим
вкладеним вмістом і malformed CMS — відхиляються без падіння; підписана квитанція VERIFY-иться
назад і містить очікуваний `<TICKET>`.

Туди ж заведено **зовнішнього суддю** (`iit_verify_x86.exe`, нативна бібліотека АТ «ІІТ»), але
з поточним тест-ключем він **нічого не доводить** і чесно віддає *іменований skip*: сертифікат
`test-diia.p12` прострочений, тож арбітр відхиляє добрий і зіпсований CMS однаково — на строку дії
(код `52`), ще до перевірки гешів. Умова, за якої цю межу можна закрити, — у
[architecture/uapki.md](../architecture/uapki.md) §8.4.

> **Межі перевірки: оракул СЛАБШИЙ за реальний сервер ДПС.** Консоль валідує в режимі
> `validationType:"STRUCT"` (офлайн, бо тест-ланцюг прострочений), а STRUCT перевіряє **лише
> структуру підпису**: підпис над `signedAttributes` і геш вкладеного вмісту. Він **НЕ** валідує
> ланцюг сертифікатів, **не перевіряє строк дії сертифіката** (§4.5), не ходить в OCSP/CRL і не
> перевіряє підпис ЦСК на вкладеному сертифікаті.
> Практичний наслідок: зміна байтів **поза підписаною областю** (наприклад, усередині вкладеного
> сертифіката — перевірено на CRL-URL) лишає вердикт `TOTAL-VALID`, бо підпис і геш вмісту справді
> цілі. Реальний ФС ДПС таке відхилив би на перевірці сертифіката (`12 CryptographyError`).
> Тому `TOTAL-VALID` від оракула означає «підпис і вміст не підмінені», а **не** «документ прийме
> ДПС». Псування самого вмісту чи підпису оракул ловить надійно (див. `--self-test`).

**Тестова обробка 1С** — `ExtDataProcessors/SimplyAddinConnect_test`, модуль форми
[`SimplyAddinConnect/Forms/Форма/Ext/Form/Module.bsl`](../../ExtDataProcessors/SimplyAddinConnect_test/SimplyAddinConnect/Forms/Форма/Ext/Form/Module.bsl).

Регіон `UAPKI` — прямі кнопки компоненти: «Створити обʼєкт UAPKI» + 11 методів життєвого циклу
(`VERSION`, `INIT`, `PROVIDERS`, `OPEN`, `KEYS`, `SELECT_KEY`, `SIGN`, `VERIFY`, `DIGEST`, `CLOSE`,
`DEINIT`); параметри беруться з реквізитів форми `ШляхДоКлючаUAPKI`, `ПарольКлючаUAPKI`,
`ІдКлючаUAPKI`, відповідь лягає в `РезультатUAPKI`.

Регіон `UAPKI_HTTP` — раунд-тріп проти цієї консолі:
- **«Підписати й відправити на валідацію»** — SIGN компонентою → POST `/doc` → отримана квитанція
  розгортається назад компонентою (VERIFY + `content.bytes`) → показ `ticket01`.
- **«Запитати еталон і перевірити»** — GET `/reference?type=check` → VERIFY+видобуток компонентою
  → показ вмісту еталона (позитивний результат — лише коли консоль запущено **без** `--canned`;
  у canned-режимі очікувано `4161`, див. врізку вище).

---

## 10. Типові помилки й рішення

| Симптом | Причина / рішення |
|---|---|
| `INIT` → `countCmProviders = 0` | Провайдер не завантажився (антивірус/права на `%LOCALAPPDATA%`) — `OPEN`/`SIGN` не працюватимуть (§5) |
| Повторний `INIT` → помилка | `RET_UAPKI_ALREADY_INITIALIZED` — `INIT` робиться раз на процес; тримай об'єкт живим, не ре-ініціалізуй (§4) |
| `SIGN`/`OPEN` → `errorCode:500` | Виняток компоненти: перевір формат JSON-параметрів (вкладені структури — лише «сирим» JSON, §2) |
| CAdES-T → `4120` в офлайні | `RET_UAPKI_OFFLINE_MODE` — формат із часом потребує TSP/онлайн; для офлайну бери CAdES-BES (§6) |
| `VERIFY` `errorCode:0`, але `statusSignature` `VALID` при битому контенті | Дивись `signatureInfos[0].status` (`TOTAL-VALID`), а не `statusSignature` (§4.5) |
| `SELECT_KEY` → `errorCode:0`, а наступний `SIGN` з `includeCert:true` → `4161` | Ключ нового зразка: SKI сертифіката на Купині, і за старим `id` сертифікат не знаходиться. Бери `keys[].keyId2` і перевіряй **наявність `certId`** у відповіді `SELECT_KEY`, а не `errorCode` (§4.3) |
| `VERIFY` → `TOTAL-VALID`, але незалежний двигун той самий підпис відхиляє | Режим `STRUCT` не перевіряє ані строк дії сертифіката, ані ланцюг: `TOTAL-VALID` = «структурно валідний», а не «юридично чинний» (§4.5) |
| `VERIFY` еталона консолі → `errorCode:4161` | `CERT_NOT_FOUND`: консоль запущено з `--canned` і віддала справжній `.signed` ДПС — це CAdES-T, а сертифіката TSP-сервера АЦСК немає. Очікувано; для позитивного сценарію запускай консоль без `--canned` (§9.1) |
| 1С вантажить стару версію компоненти | Кеш нативної компоненти — [README §2.2](README.md) |

---

## 11. Пов'язане

- [README.md](README.md) — спільні механізми (підключення DLL, кеш, лог).
- [docs/architecture/uapki.md](../architecture/uapki.md) — внутрішня будова стеку UAPKI
  (ланцюг `process()`, гібридна збірка, вбудований провайдер, потрійний пошук каталогу).
- [`extern/uapki/doc/UAPKI-PM-2.0.16.md`](../../extern/uapki/doc/UAPKI-PM-2.0.16.md) — авторитетна
  настанова програміста UAPKI (усі методи, параметри, коди помилок; є й англійська версія).
- Скіли `.claude/skills/`: `uapki-integration`, `ecp-testing-without-1c`, `prro-fiscal` — переносне ноу-хау.
