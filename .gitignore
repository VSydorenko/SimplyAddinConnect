/build_*/
/build64Lin
/build32Lin
/build64
/build32
/build
/build_tests
/tools
/bin
/.vc
/.vs
/.vscode
/tmp
.build/
manifest.xml
*.o
*.d
*.so

# Локальний конфіг реальних ключів: шляхи й паролі. НІКОЛИ не комітити.
tests/data/local-keys.json

# Робочі дерева сесій Claude Code (git worktree) — окремі чекаути, не частина репозиторію.
# Без цього правила `git add -A` затягнув би їх у коміт як вкладений репозиторій.
/.claude/worktrees/
